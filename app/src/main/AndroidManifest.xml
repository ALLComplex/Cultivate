<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cultivate.juniordesign.cultivate">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CultivateTheme">

        <activity
            android:name=".ActivityClass.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityClass.EventActivity"></activity>
        <activity
            android:name=".ActivityClass.MyGroupsActivity">
        </activity>
        <activity
            android:name=".ActivityClass.MyProfileActivity">
        </activity>
        <activity
            android:name=".ActivityClass.MainActivity">
        </activity>
        <activity
            android:name=".ActivityClass.RegistrationActivity">
        </activity>
        <activity android:name=".ActivityClass.CreateGroupActivity"></activity>
        <activity android:name=".ActivityClass.JoinGroupActivity"></activity>
        <activity android:name=".ActivityClass.GroupProfileActivity"></activity>
        <activity android:name=".ActivityClass.MembersListActivity"></activity>
        <activity android:name=".ActivityClass.CreateEventActivity"></activity>
        <activity android:name=".ActivityClass.MyEventsActivity"></activity>
    </application>

</manifest>